<div class="content">
    <div class="actions right text-right">
      <a class="button small" href="/{{store.id}}/pages">Publish Later</a>
    </div>
    <h4>Publish Page</h4>
    <div class="page-edit panel">
        <p>
            The page will be live immediately:
            <a href="{{store.public-base-url}}{{page.url}}">
                {{store.public-base-url}}{{page.url}}
            </a>
        </p>
        <p>
            It will overwrite the existing page.
        </p>
        <div class="actions row">
            <a class="publish js-next button small" href="">Publish Page</a>
        </div>
        <div class="row fail">
            <span class="alert label">Error</span>
            Could not publish page. Please try again later.
        </div>
    </div>
</div>