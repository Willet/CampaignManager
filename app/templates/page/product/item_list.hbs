<div class="product-image">
  <img src="{{#if default-image}}{{default-image.images.large.url}}{{else}}http://placehold.it/252x252&text=No%20Image%20Available{{/if}}" />
</div>
<div class="product-wrapper">
  <div class="product-info">
    <label for="name" class="product-label">Name</label>
    <span class="product-label-text"><a href="{{url}}">{{default name "(No Name)"}}</a></span>
    <label for="price" class="product-label">Price</label>
    <span class="product-label-text"><b>{{default price "(Price Missing)"}}</b></span>
    <label for="description" class="product-label">Description</label>
    <p class="product-label-text description">{{default description "(No Description)"}}</p>
  </div>
  <div class="product-actions">
    {{#is page-status 'added'}}
    <button class="remove-product-button js-product-remove">
      Remove <i class="icon-general-error"></i>
    </button>
    {{else}}
    <button class="add-product-button js-product-add">
      Add <i class="icon-general-checkmark"></i>
    </button>
    {{/is}}
    {{#if page-prioritized}}
    <button class="deprioritize-product-button js-product-deprioritize">
      Deprioritize <i class="icon-general-down-arrow"></i>
    </button>
    {{else}}
    <button class="prioritize-product-button js-product-prioritize">
      Prioritize <i class="icon-general-up-arrow"></i>
    </button>
    {{/if}}
    <button class="preview-product-button js-product-preview">
      Preview <i class="icon-accessibility-eyeball"></i>
    </button>
  </div>
</div>
{{#is page-status 'added'}}
  {{#if page-prioritized}}
      <div class="product-state product-state-prioritized">
        Prioritized <i class="icon-general-up-arrow"></i>
      </div>
    {{else}}
      <div class="product-state product-state-added">
        Added <i class="icon-general-checkmark"></i>
      </div>
    {{/if}}
{{/is}}
